<%= form_with(model: @log_entry, local: false, remote: true) do |form| %>
  <% if @log_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@log_entry.errors.count, "error") %> prohibited this log_entry from being saved:</h2>

      <ul>
        <% @log_entry.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.text_field :arrival, size: 5, title: 'Arrival' %>
    <%= form.text_field :departure, size: 5, title: 'Departure' %>
    <%= form.text_field :place, size: 20, title: 'Place' %>
    <%= form.text_field :address, size: 20, title: 'Address' %>
    <%= form.select :people_num, options_for_select(LogEntry.num_options, form.object.people_num), {}, class: '', title: 'Number of contacts' %>
    <%= form.text_field :persons, size: 20, title: 'Names of contacts' %>

    <%= form.submit 'Save' %>
<% end %>
